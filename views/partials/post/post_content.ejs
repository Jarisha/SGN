<div class="page_pin">
  <div class="page_header clearfix">
    <a href="/user/{{pagePin.poster || pagePin.posterName}}">
      <img style="float: left; height: 50px; width: 50px;" ng-src="{{pagePin.profileImg || '/images/30x30.gif'}}"/>
        <p style="font-size: 25px; margin-left: 60px;" >{{pagePin.userName}}</p>
    </a>
    <p style="font-size: 14px; line-height: 20px; margin-left: 60px; margin-top: 7px;" >posted onto {{pagePin.category}}</p>
  </div>
  <div class="page_body">
    <div class="page_content">
      <p>{{ pagePin.gameName }}</p>
      <img ng-src="{{pagePin.sourceUrl}}"/>
    </div>
    <div class="page_desc">
      <p>{{pagePin.description}}</p>
    </div>
  </div>
  <div class="page_footer">
    <div ng-repeat="cmt in pagePin.comments" class="page_comments">
      <img ng-src="{{cmt.posterImg || '/images/30x30.gif'}}" width="40" height="40" />
      <p class="post_text">
        <a href="/user/{{cmt.posterName}}"><b>{{cmt.posterName}}</b></a>
        </br>
        {{ cmt.content }}
      </p>
    </div>
    <div class="view_respond clearfix">
      <a href="#"><img ng-src="{{userImg}}" class="profile_img"/></a>
      <textarea ng-click="showRespond = true" class="view_respond_txtarea" placeholder="Post comment" tabindex="" ng-model="big_text"></textarea>
      <button ng-show="showRespond" ng-click="addBigComment(big_text, bigPin.index); showRespond = false;" class="view_respond_btn" type="button">Comment</button>
    </div>
  </div>
</div>

<!--<div class="game_pin">
  <div class="game_options hidden">
    <a ng-show="!pin.likedFlag" class="like_btn" ng-click="like(pin)" >Like</a>
    <a ng-show="pin.likedFlag" class="like_btn" ng-click="unlike(pin)" >Unlike</a>
  </div>
  <a class="view_trigger" ng-click="viewBigPin($index)" data-toggle="dropdown">
    <img class="game_img" ng-src="pin.imageUrl || pin.sourceUrl" width="220"/>
  </a>
  <a name="c1"></a>
  <p class="game_description">pin.description</p>
  <p class="game_attributes">
    <span>pin.comments.length comment<span ng-show="pin.comments.length != 1">s</span></span>&nbsp;&nbsp;&nbsp;&nbsp;
    <span>pin.likedBy.length like<span ng-show="pin.likedBy.length != 1">s</span></span>
  </p>
  <div class="posted_by">
    <img ng-src="pin.profileImg || '/images/30x30.gif'" class="profile_img" width="30" height="30" /><p
    class="post_text"><a href="/user/pin.poster || pin.posterName"><b>pin.poster || pin.posterName</b></a> 
                       onto pin.category
    </p>
  </div>
  <div ng-repeat="cmt in pin.comments" class="comment">
    <img ng-src="cmt.posterImg || '/images/30x30.gif'" class="profile_img" width="30" height="30" /><p 
    class="post_text"><a href="/user/cmt.posterName"><b>cmt.posterName</b></a>
                       cmt.content
    </p>
  </div>
  <div class="respond clearfix" tabindex="">
      <img ng-src="userImg || '/images/30x30.gif'" class="profile_img" width="30" height="30" /><textarea 
      ng-click="showComment = true; masonry();" class="respond_txtarea" placeholder="Post comment" tabindex="" ng-model="text"></textarea>
      <button ng-show="showComment" class="respond_btn" type="submit" ng-click="addComment(text, $index); text=null; showComment = false">Comment</button>
  </div>
</div>-->