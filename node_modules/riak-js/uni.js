db = require('./lib').getClient();

db.save('unicode', 'test', 'tét', function() {
  db.get('unicode', 'test', function(err, data, meta) {
    console.log(meta);
    console.log(data)
  });
});

